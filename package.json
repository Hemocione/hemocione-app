{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "capacitor:build": "nuxt generate --no-ssr && npx cap sync"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.6.0",
    "@nuxtjs/google-fonts": "^3.0.1",
    "nuxt": "^3.9.3"
  },
  "dependencies": {
    "@capacitor/android": "^5.6.0",
    "@capacitor/core": "^5.6.0",
    "@capacitor/ios": "^5.6.0"
  },
  "name": "hemocione.app",
  "version": "0.0.1"
}
